spring.thymeleaf.prefix: classpath:/template/
spring.thymeleaf.suffix: .html
spring.thymeleaf.mode: LEGACYHTML5
spring.thymeleaf.cache: true

security.basic.enabled: false

server:
    port: 8081

# OpenIoT 配置
bupt.thingsboard:
  host: 10.108.216.193 # for cc
#  host: 10.108.217.227 # for gpu
  host: localhost
  port: 8080
  login_url: http://${bupt.thingsboard.host}:${bupt.thingsboard.port}/api/auth/login